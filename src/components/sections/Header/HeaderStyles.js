import styled from "styled-components";

export const Container = styled.header`
  h1 {
    padding-top: 40px;
    padding-bottom: 50px;
    font-weight: 600;
    font-size: clamp(3.5rem, 8vw, 4.69rem);
    line-height: clamp(4.27, 8vw, 5.69rem);
    text-transform: uppercase;
  }
`;
